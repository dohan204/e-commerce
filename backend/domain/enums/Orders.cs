namespace domain.enums
{
    public enum StatusOrdere {pending, confirmed, shipping, delivered, cancelled}
    public enum PaymentMethodOrder {cod, vnpay, momo, card}
    public enum PaymentStatusOrder {unpaid, paid, refunded}
}